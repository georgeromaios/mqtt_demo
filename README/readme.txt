----------------------------------------------------------------------READ ME----------------------------------------------------------------------
*The Mosquitto MQTT broker is needed to run the code.
*Set up a broker.conf file
*Set users and passwors(optional)
*Run the program as Spring Boot App
*Open CMD and navigate to the mosquito broker
*Enter mosquitto_pub -t (tile from "1" to "5") -m "your message". The title and message should appear in the console